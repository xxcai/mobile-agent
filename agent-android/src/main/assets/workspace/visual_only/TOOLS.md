# Tool Usage

- Use `android_view_context_tool` to inspect the current interface.
- Use `android_gesture_tool` for native UI actions after observation.
- Use `android_web_action_tool` for Web DOM actions after observation.
