---
status: testing
phase: v15-02-cpp-tooling
source: v15-02-01-SUMMARY.md
started: 2026-03-05T12:10:00Z
updated: 2026-03-05T12:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. C++ Build Verification
expected: C++ code compiles without errors after modifications. Run: ./gradlew :agent:compileDebugCpp
result: pass

### 2. JNI Method Connection
expected: |
  NativeAgent.java can call nativeSetToolsSchema without errors. Check logcat for JNI method resolution.

  **测试步骤：**
  1. 安装 APK 到手机：`adb install -r app/build/outputs/apk/debug/app-debug.apk`
  2. 启动 app 并连接 Nanobot
  3. 查看 logcat：`adb logcat | grep -i "nativeSetToolsSchema"`
  4. 预期：无 JNI 错误，日志显示 "Successfully registered tools from schema"
result: pass

### 3. tools.json Loading
expected: |
  NativeNanobotApi reads tools.json from assets and passes to native layer without errors.

  **测试步骤：**
  1. 重新编译并安装 APK
  2. 启动 app
  3. 查看 logcat：`adb logcat | grep -i "NativeNanobotApi"`
  4. 预期：日志显示 "Successfully loaded and passed tools.json to native layer"
result: pass

### 4. Tool Registry Integration
expected: |
  ToolRegistry receives and parses tools schema correctly. Only call_android_tool exposed to LLM.

  **测试步骤：**
  1. 在手机上启动 Mobile Agent app
  2. 发送消息："请列出你有哪些工具可用"
  3. 查看 LLM 的响应：
     - 预期：LLM 只提到 `call_android_tool`
     - 预期：LLM **不**提到 `show_toast`、`read_clipboard` 等作为独立工具
result: pass

### 5. show_toast Removed from Builtins
expected: |
  show_toast is NOT in the built-in tools list (only accessible via call_android_tool).

  **测试步骤：**
  1. 在手机上打开 Mobile Agent app
  2. 在聊天框输入："请显示一个 toast 消息，内容写 '测试成功'"
  3. 发送消息
  4. 观察 LLM 的响应和手机上的行为：
     - 预期：手机屏幕上显示 toast 消息 "测试成功"
     - 预期：LLM 使用了 `call_android_tool` 工具来调用
  5. 查看 app 日志（可选）：`adb logcat | grep -i "call_tool"`
result: pass

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
