---
phase: v16-01
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/main/cpp/include/icraw/types.hpp
  - agent/src/main/cpp/src/core/skill_loader.cpp
  - cxxplatform/tests/skill_loader.test.cpp
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03

must_haves:
  truths:
    - "Skill 可以在 SKILL.md 中定义 dependencies 字段"
    - "C++ 层能够解析并加载带依赖的 Skills"
    - "Skills 按依赖顺序加载，无循环依赖时报错"
  artifacts:
    - path: "agent/src/main/cpp/include/icraw/types.hpp"
      provides: "SkillMetadata 结构体定义"
      contains: "dependencies: std::vector<std::string>"
    - path: "agent/src/main/cpp/src/core/skill_loader.cpp"
      provides: "Skill 加载和依赖解析"
      exports: ["load_skills", "resolve_skill_dependencies"]
    - path: "cxxplatform/tests/skill_loader.test.cpp"
      provides: "依赖解析单元测试"
      contains: "TEST_CASE.*dependencies"
  key_links:
    - from: "skill_loader.cpp"
      to: "types.hpp"
      via: "SkillMetadata.dependencies"
      pattern: "skill\\.dependencies"
---

<objective>
实现自定义 Skills 的定义和加载机制，包括 Skill 配置文件格式定义、C++ 层 Skills 加载机制、Skills 之间的依赖关系处理。
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caixiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/v16-01-skills/v16-01-CONTEXT.md
@.planning/phases/v16-01-skills/v16-01-RESEARCH.md

## Key Implementation Details (from Context)

### Existing Code (DO NOT MODIFY):
- `agent/src/main/cpp/src/core/skill_loader.cpp` - 完整的 Skill 加载实现
- `agent/src/main/cpp/include/icraw/core/skill_loader.hpp` - SkillLoader 头文件
- 配置格式: YAML frontmatter + Markdown (SKILL.md)
- 加载路径: workspace/skills/ + extra_dirs

### SkillMetadata (types.hpp line 260-272):
```cpp
struct SkillMetadata {
    std::string name;
    std::string description;
    std::vector<std::string> required_bins;
    std::vector<std::string> required_envs;
    std::vector<std::string> any_bins;
    std::vector<std::string> config_files;
    std::vector<std::string> os_restrict;
    bool always = false;
    std::string primary_env;
    std::string emoji;
    std::string content;
    // MISSING: dependencies
};
```

### YAML Parsing (skill_loader.cpp line 251-289):
现有代码解析 YAML frontmatter 字段 (description, requiredBins, requiredEnvs, anyBins, os, always, emoji)。
需要添加 dependencies 字段解析。

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies field to SkillMetadata</name>
  <files>agent/src/main/cpp/include/icraw/types.hpp</files>
  <behavior>
    - SkillMetadata 结构体包含 dependencies 字段
    - 字段类型为 std::vector<std::string>
    - 默认初始化为空 vector
  </behavior>
  <action>
在 SkillMetadata 结构体中添加 `std::vector<std::string> dependencies;` 字段。位置：在 content 字段之后，结构体结束前。
  </action>
  <verify>
    <automated>grep -n "dependencies" /Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/include/icraw/types.hpp</automated>
  </verify>
  <done>SkillMetadata 包含 dependencies 字段</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse dependencies from YAML frontmatter</name>
  <files>agent/src/main/cpp/src/core/skill_loader.cpp</files>
  <behavior>
    - 解析 SKILL.md 中 dependencies 字段
    - 支持数组格式: dependencies: [dep1, dep2]
    - 解析失败时跳过该 skill（与现有错误处理一致）
  </behavior>
  <action>
在 parse_skill_file 方法中添加 dependencies 解析逻辑。参考现有字段解析模式 (requiredBins, requiredEnvs 等)，在 emoji 解析之后添加：

```cpp
if (yaml_json.contains("dependencies")) {
    for (const auto& dep : yaml_json["dependencies"]) {
        skill.dependencies.push_back(dep.get<std::string>());
    }
}
```
  </action>
  <verify>
    <automated>grep -n "dependencies" /Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/src/core/skill_loader.cpp</automated>
  </verify>
  <done>dependencies 字段被正确解析到 SkillMetadata</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement topological sort for dependency resolution</name>
  <files>agent/src/main/cpp/src/core/skill_loader.cpp</files>
  <behavior>
    - 按依赖顺序返回 skills 列表
    - 检测循环依赖并报错
    - 处理不存在的依赖（警告 + 跳过）
  </behavior>
  <action>
在 SkillLoader 类中添加 resolve_skill_dependencies 方法。使用 Kahn's algorithm 实现拓扑排序：

1. 构建依赖图 (name -> dependencies)
2. 计算入度 (in-degree)
3. 使用队列处理入度为 0 的节点
4. 检测循环：结果数量 != 输入数量时报错
5. 处理缺失依赖：记录警告日志

在 skill_loader.hpp 中添加方法声明：
```cpp
// Resolve skill dependencies using topological sort
std::vector<SkillMetadata> resolve_skill_dependencies(
    std::vector<SkillMetadata>& skills) const;
```

在 load_skills 方法末尾调用 resolve_skill_dependencies：
```cpp
// Resolve dependencies after loading all skills
all_skills = resolve_skill_dependencies(all_skills);
```
  </action>
  <verify>
    <automated>grep -n "resolve_skill_dependencies\|topological\|kahn" /Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/src/core/skill_loader.cpp</automated>
  </verify>
  <done>Skills 按依赖顺序加载，循环依赖被检测</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add unit tests for dependency resolution</name>
  <files>cxxplatform/tests/skill_loader.test.cpp</files>
  <behavior>
    - 测试解析 dependencies 字段
    - 测试拓扑排序顺序
    - 测试循环依赖检测
    - 测试缺失依赖警告
  </behavior>
  <action>
在现有测试文件中添加以下测试用例：

1. **Parse dependencies from SKILL.md**: 创建带 dependencies 的 SKILL.md，验证解析结果
2. **Topological sort ordering**: 创建 A->B->C 依赖链，验证加载顺序
3. **Circular dependency detection**: 创建 A->B->A，验证抛出异常或返回错误
4. **Missing dependency warning**: 创建依赖不存在的 skill，验证警告日志

测试使用 temp_directory_path 创建临时 skill 目录。
  </action>
  <verify>
    <automated>cd /Users/caixiao/Workspace/projects/mobile-agent/cxxplatform && cmake -B build -DCMAKE_TOOLCHAIN_FILE=../vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=release && cmake --build build --target skill_loader.test 2>/dev/null || echo "Build test binary first"</automated>
  </verify>
  <done>依赖解析相关测试通过</done>
</task>

</tasks>

<verification>
1. 编译测试: `cmake --build build --target skill_loader.test`
2. 运行测试: `ctest -R skill_loader -V`
3. 验证 dependencies 字段存在于 types.hpp
4. 验证 skill_loader.cpp 包含依赖解析逻辑
</verification>

<success_criteria>
1. SKILL.md 中可以定义 dependencies 字段
2. C++ 层能够加载带依赖的 Skills
3. Skills 按拓扑顺序加载
4. 循环依赖被检测并报错
5. 单元测试覆盖依赖解析功能
</success_criteria>

<output>
After completion, create `.planning/phases/v16-01-skills/v16-01-01-SUMMARY.md`
</output>
