---
phase: v20-03-code-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/main/java/com/hh/agent/library/api/NativeMobileAgentApi.java
  - agent/build.gradle
  - app/src/main/java/com/hh/agent/presenter/NativeMobileAgentApiAdapter.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "agent 模块不含任何 Android 依赖，可打包为纯 Java AAR"
    - "平台相关工具已在 app 模块实现"
    - "编译通过，无错误"
  artifacts:
    - path: "agent/src/main/java/com/hh/agent/library/api/NativeMobileAgentApi.java"
      provides: "纯 Java API 接口，无 Android 依赖"
    - path: "agent/build.gradle"
      provides: "无 Android 依赖的构建配置"
  key_links:
    - from: "app/src/main/java/com/hh/agent/presenter/NativeMobileAgentApiAdapter.java"
      to: "agent/src/main/java/com/hh/agent/library/api/NativeMobileAgentApi.java"
      via: "nativeApi.initialize(inputStream, configJson)"
---

<objective>
修复 agent 模块中的 Android 依赖，实现纯 Java AAR 打包目标。

Purpose: 移除 NativeMobileAgentApi.java 中的 Android API 调用，移除 agent/build.gradle 中的 Android 依赖
Output: agent 模块不含任何 Android 依赖
</objective>

<context>
@.planning/phases/v20-03-code-migration/v20-03-CONTEXT.md
@.planning/phases/v20-03-code-migration/v20-03-01-VERIFICATION.md

Gap source: v20-03-01-VERIFICATION.md
- NativeMobileAgentApi.java 第 3 行: `import android.content.Context`
- NativeMobileAgentApi.java 第 71,73,77,101 行: `android.util.Log.*` 调用
- NativeMobileAgentApi.java 第 56,89,91 行: `context.getAssets()` 调用
- agent/build.gradle 第 55-56 行: androidx.appcompat 和 androidx.core 依赖

Existing code to reference:
- app/src/main/java/com/hh/agent/presenter/NativeMobileAgentApiAdapter.java (caller needs update)
- agent/build.gradle (dependencies to remove)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor NativeMobileAgentApi to use pure Java</name>
  <files>agent/src/main/java/com/hh/agent/library/api/NativeMobileAgentApi.java</files>
  <action>
1. Remove line 3: `import android.content.Context;`
2. Add import: `import java.io.InputStream;`
3. Change method signature at line 56:
   - From: `public synchronized void initialize(Context context, String configPath)`
   - To: `public synchronized void initialize(InputStream toolsStream, String configPath)`
4. Replace all android.util.Log calls:
   - Line 71: `android.util.Log.i("NativeMobileAgentApi", "...")` -> `System.out.println("[NativeMobileAgentApi] " + "...");`
   - Line 73: `android.util.Log.w("NativeMobileAgentApi", "...")` -> `System.out.println("[NativeMobileAgentApi] " + "...");`
   - Line 77: `android.util.Log.w("NativeMobileAgentApi", "...")` -> `System.out.println("[NativeMobileAgentApi] " + "...");`
   - Line 101: `android.util.Log.e("NativeMobileAgentApi", "...")` -> `System.err.println("[NativeMobileAgentApi] " + "...");`
5. Update loadToolsFromAssets method:
   - Change method signature from `private String loadToolsFromAssets(Context context)` to `private String loadToolsFromAssets(InputStream toolsStream)`
   - Replace `context.getAssets().open("tools.json")` with direct use of the passed InputStream parameter
   - The method should read from the InputStream directly instead of opening assets
6. Update the initialize method body:
   - Replace `if (context != null)` with `if (toolsStream != null)`
   - Replace `loadToolsFromAssets(context)` with `loadToolsFromAssets(toolsStream)`
  </action>
  <verify>
<automated>grep -n "android\." /Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/java/com/hh/agent/library/api/NativeMobileAgentApi.java</automated>
  </verify>
  <done>NativeMobileAgentApi.java 无任何 android.* 导入和调用</done>
</task>

<task type="auto">
  <name>Task 2: Remove Android dependencies from agent/build.gradle</name>
  <files>agent/build.gradle</files>
  <action>
1. Remove lines 55-56:
   ```
   implementation 'androidx.appcompat:appcompat:1.6.1'
   implementation 'androidx.core:core:1.12.0'
   ```
2. Keep the rest of the dependencies and configuration (NDK, CMake, etc.)
  </action>
  <verify>
<automated>grep -n "androidx\|appcompat\|core:" /Users/caixiao/Workspace/projects/mobile-agent/agent/build.gradle</automated>
  </verify>
  <done>agent/build.gradle 无 Android 依赖</done>
</task>

<task type="auto">
  <name>Task 3: Update NativeMobileAgentApiAdapter to pass InputStream</name>
  <files>app/src/main/java/com/hh/agent/presenter/NativeMobileAgentApiAdapter.java</files>
  <action>
1. Add import: `import android.content.Context;` (keep for other uses)
2. At line 76, change:
   - From: `nativeApi.initialize(context, configJson);`
   - To:
     ```
     // Open tools.json from assets and pass InputStream to agent
     InputStream toolsStream = null;
     if (context != null) {
         try {
             toolsStream = context.getAssets().open("tools.json");
         } catch (Exception e) {
             System.out.println("[NativeMobileAgentApiAdapter] Failed to open tools.json: " + e.getMessage());
         }
     }
     nativeApi.initialize(toolsStream, configJson);
     ```
  </action>
  <verify>
<parameter name="automated">grep -n "nativeApi.initialize" /Users/caixiao/Workspace/projects/mobile-agent/app/src/main/java/com/hh/agent/presenter/NativeMobileAgentApiAdapter.java</parameter>
  </verify>
  <done>NativeMobileAgentApiAdapter passes InputStream to NativeMobileAgentApi</done>
</task>

<task type="auto">
  <name>Task 4: Verify compilation</name>
  <files></files>
  <action>
Run the build to verify everything compiles correctly:
```
./gradlew assembleDebug 2>&1 | tail -30
```
  </action>
  <verify>
<parameter name="automated">./gradlew assembleDebug 2>&1 | tail -10</parameter>
  </verify>
  <done>Build succeeds with no errors</done>
</task>

</tasks>

<verification>
- agent 模块无任何 android.* 导入
- agent/build.gradle 无 androidx 依赖
- ./gradlew assembleDebug 成功
</verification>

<success_criteria>
1. NativeMobileAgentApi.java 不含任何 Android API 调用
2. agent/build.gradle 无 Android 依赖
3. 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/v20-03-code-migration/v20-03-02-SUMMARY.md`
</output>
