---
phase: 05-api-key
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - /Users/caixiao/Workspace/projects/mobile-agent/agent-android/src/main/java/com/hh/agent/android/WorkspaceManager.java
autonomous: true
requirements: []
---

<objective>
修改 WorkspaceManager 初始化逻辑，实现 Skills 混合模式（内置+用户自定义共存）。
</objective>

<context>
根据 CONTEXT.md 的决策：
- 统一目录：/sdcard/Android/data/com.hh.agent/files/.icraw/workspace/skills/
- 初始化：首次启动从 assets 复制内置 skills，跳过已有文件
- 重名时保留用户版本（首次复制时跳过）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 Skills 初始化逻辑</name>
  <files>/Users/caixiao/Workspace/projects/mobile-agent/agent-android/src/main/java/com/hh/agent/android/WorkspaceManager.java</files>
  <action>
修改 WorkspaceManager.initialize() 中的 skills 复制逻辑：

1. 修改 copyAssetsToWorkspace() 方法：
   - 遍历内置 skills 目录（assets/workspace/skills/）
   - 对每个 skill 检查目标目录是否存在
   - 如果目标不存在则复制，已存在则跳过

2. 伪代码：
```java
// 内置 skills 列表
String[] builtInSkills = {"im_sender", "chinese_writer"};

for (String skillName : builtInSkills) {
    File targetDir = new File(skillsDir, skillName);
    if (!targetDir.exists()) {
        copyFromAssets("workspace/skills/" + skillName, targetDir);
    }
}
```

3. 修改 copyAssetDirectory() 支持覆盖判断：
   - 在复制前检查目标是否存在
   - 存在则跳过，不存在则复制
  </action>
  <verify>
<automated>grep -c "builtInSkills\|copyFromAssets" /Users/caixiao/Workspace/projects/mobile-agent/agent-android/src/main/java/com/hh/agent/android/WorkspaceManager.java</automated>
  </verify>
  <done>首次启动时复制内置 Skills，用户已存在的 Skill 不会被覆盖</done>
</task>

</tasks>

<verification>
编译通过，初始化逻辑正确
</verification>

<success_criteria>
用户可以在 {外部存储}/.icraw/workspace/skills/ 目录下添加自定义 Skills，内置 Skills 首次复制后不覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-key/05-04-SUMMARY.md`
</output>
