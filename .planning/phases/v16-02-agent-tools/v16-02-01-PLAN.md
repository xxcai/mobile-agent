---
phase: v16-02-agent-tools
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - app/src/main/assets/tools.json
  - agent/src/main/java/com/hh/agent/library/tools/SearchContactsTool.java
  - agent/src/main/java/com/hh/agent/library/tools/SendImMessageTool.java
  - agent/src/main/java/com/hh/agent/library/AndroidToolManager.java
  - agent/src/main/assets/workspace/skills/im_sender/SKILL.md
autonomous: true
requirements:
  - CALL-01
  - CALL-02
  - CALL-03

must_haves:
  truths:
    - Agent 能够通过 Skill 解析调用 Android Tools
    - 多步骤 Tool 调用链能够顺序执行
    - Tool 执行结果能够返回给 Agent
  artifacts:
    - path: app/src/main/assets/tools.json
      provides: Tool schema definition with new enum values
      contains: search_contacts, send_im_message
    - path: agent/src/main/java/com/hh/agent/library/tools/SearchContactsTool.java
      provides: Mock contact search implementation
      implements: ToolExecutor interface
    - path: agent/src/main/java/com/hh/agent/library/tools/SendImMessageTool.java
      provides: Mock IM message sending implementation
      implements: ToolExecutor interface
    - path: agent/src/main/java/com/hh/agent/library/AndroidToolManager.java
      provides: Tool registration and routing
      registers: search_contacts, send_im_message
    - path: agent/src/main/assets/workspace/skills/im_sender/SKILL.md
      provides: Test skill definition for im_sender
      contains: multi-step workflow description
  key_links:
    - from: tools.json
      to: AndroidToolManager
      via: enum value -> tools.put() registration
      pattern: search_contacts -> new SearchContactsTool
    - from: AndroidToolManager
      to: SearchContactsTool
      via: tools.get() -> executor.execute()
      pattern: call_tool.*SearchContactsTool
    - from: Skill (im_sender)
      to: Tools
      via: LLM interprets SKILL.md and calls tools
      pattern: search_contacts -> send_im_message chain
---

<objective>
实现 Agent 通过 Skill 调用 Android Tools 的能力

Purpose: 让 Agent 能够解析 Skill 定义，调用对应的 Android Tools，支持多步骤 Tool 调用链，并正确处理 Tool 结果

Output: 扩展的工具系统 + im_sender 测试 Skill
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caixiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing code patterns to follow:

From app/src/main/assets/tools.json:
```json
{
  "version": 2,
  "tools": [{
    "type": "function",
    "function": {
      "name": "call_android_tool",
      "enum": ["display_notification", "show_toast", "read_clipboard", "take_screenshot"]
    }
  }]
}
```

From agent/src/main/java/com/hh/agent/library/AndroidToolManager.java:
```java
public void initialize() {
    tools.put("show_toast", new ShowToastTool(getActivity()));
    tools.put("display_notification", new DisplayNotificationTool(getActivity()));
}
```

From agent/src/main/java/com/hh/agent/library/tools/ShowToastTool.java:
```java
public class ShowToastTool implements ToolExecutor {
    @Override
    public String getName() { return "show_toast"; }
    @Override
    public String execute(JSONObject args) {
        return "{\"success\": true, \"result\": \"...\"}";
    }
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new tools to tools.json enum</name>
  <files>app/src/main/assets/tools.json</files>
  <action>
    Update tools.json to add search_contacts and send_im_message to the call_android_tool enum.

    Changes required:
    - Add "search_contacts" to enum array
    - Add "send_im_message" to enum array
    - Update description to include new tools

    New enum: ["display_notification", "show_toast", "read_clipboard", "take_screenshot", "search_contacts", "send_im_message"]
  </action>
  <verify>
    <automated>grep -q "search_contacts" app/src/main/assets/tools.json && grep -q "send_im_message" app/src/main/assets/tools.json</automated>
  </verify>
  <done>tools.json contains search_contacts and send_im_message in enum</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchContactsTool.java</name>
  <files>agent/src/main/java/com/hh/agent/library/tools/SearchContactsTool.java</files>
  <action>
    Create SearchContactsTool.java implementing ToolExecutor interface.

    Implementation:
    - getName() returns "search_contacts"
    - execute() accepts JSONObject args with "query" field
    - Mock data (support two test scenarios):

      **Scenario 1 - Duplicate names (multiple matches):**
      - Query "张三" returns: [{"name": "张三", "id": "001", "department": "技术部"}, {"name": "张三", "id": "002", "department": "市场部"}]

      **Scenario 2 - Unique name (single match):**
      - Query "李四" returns: [{"name": "李四", "id": "003", "department": "产品部"}]
      - Any other query returns empty array: []

    - Return format: {"success": true, "result": [...]}
    - Error handling: Return {"success": false, "error": "...", "message": "..."} on exception
    - Follow pattern from ShowToastTool.java for error handling

    Mock data requirements (per user locked decision):
    - 多个匹配项时返回所有结果，让用户选择
    - 单个匹配项直接使用
  </action>
  <verify>
    <automated>grep -q "class SearchContactsTool" agent/src/main/java/com/hh/agent/library/tools/SearchContactsTool.java</automated>
  </verify>
  <done>SearchContactsTool.java exists and implements ToolExecutor</done>
</task>

<task type="auto">
  <name>Task 3: Create SendImMessageTool.java</name>
  <files>agent/src/main/java/com/hh/agent/library/tools/SendImMessageTool.java</files>
  <action>
    Create SendImMessageTool.java implementing ToolExecutor interface.

    Implementation:
    - getName() returns "send_im_message"
    - execute() accepts JSONObject args with "contact_id" and "message" fields
    - Mock implementation: Simply return success with formatted message
    - Return format: {"success": true, "result": "消息已发送给 张三(001)"}
    - Error handling: Return {"success": false, "error": "...", "message": "..."} on exception
    - Validate required params: contact_id, message
  </action>
  <verify>
    <automated>grep -q "class SendImMessageTool" agent/src/main/java/com/hh/agent/library/tools/SendImMessageTool.java</automated>
  </verify>
  <done>SendImMessageTool.java exists and implements ToolExecutor</done>
</task>

<task type="auto">
  <name>Task 4: Register new tools in AndroidToolManager</name>
  <files>agent/src/main/java/com/hh/agent/library/AndroidToolManager.java</files>
  <action>
    Register the new tools in AndroidToolManager.initialize() method.

    Changes required:
    - Add import for SearchContactsTool
    - Add import for SendImMessageTool
    - Add tools.put("search_contacts", new SearchContactsTool(getActivity()))
    - Add tools.put("send_im_message", new SendImMessageTool(getActivity()))
    - Update log message to show registered tool count
  </action>
  <verify>
    <automated>grep -q "search_contacts" agent/src/main/java/com/hh/agent/library/AndroidToolManager.java</automated>
  </verify>
  <done>AndroidToolManager registers search_contacts and send_im_message tools</done>
</task>

<task type="auto">
  <name>Task 5: Create im_sender Skill definition</name>
  <files>agent/src/main/assets/workspace/skills/im_sender/SKILL.md</files>
  <action>
    Create the im_sender Skill definition in workspace/skills/im_sender/SKILL.md.

    The skill should define the workflow for sending IM messages:
    - Trigger: When user asks to send message to someone
    - Step 1: Use search_contacts tool to search for the contact
    - Step 2: Handle multiple results - return all for user selection
    - Step 3: After selection (or single result), use send_im_message tool
    - Step 4: Return result to user

    Include:
    - Frontmatter with description, emoji, always=false
    - Clear workflow steps in Chinese
    - Output format examples for both tools

    Per user locked decision:
    - 多个匹配项：同时返回所有结果，让用户选择
    - 单个匹配项：直接发送，不询问
  </action>
  <verify>
    <automated>test -f agent/src/main/assets/workspace/skills/im_sender/SKILL.md</automated>
  </verify>
  <done>im_sender SKILL.md exists with multi-step workflow definition</done>
</task>

</tasks>

<verification>
- Build compiles: ./gradlew assembleDebug
- Tools registered: Check logcat for "Registered 6 tools"
- Skill loaded: Check im_sender is available
</verification>

<success_criteria>
1. tools.json contains search_contacts and send_im_message in enum
2. SearchContactsTool and SendImMessageTool implement ToolExecutor
3. AndroidToolManager registers both new tools
4. im_sender Skill defines multi-step workflow
5. Build compiles without errors
</success_criteria>

<output>
After completion, create .planning/phases/v16-02-agent-tools/v16-02-01-SUMMARY.md
</output>
