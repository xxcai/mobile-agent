---
phase: quick-15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  truths:
    - "app模块有2处ActivityLifecycleCallbacks注册点"
    - "MainActivity注册的回调控制自身页面悬浮球显示/隐藏"
    - "AppLifecycleObserver监听应用整体前后台切换"
    - "两个回调职责不同但都与悬浮球显示相关"
  artifacts:
    - path: ".planning/quick/15-app-activitylifecyclecallbacks/analysis.md"
      provides: "代码分析报告"
  key_links:
    - from: "MainActivity.java:53"
      to: "FloatingBallManager"
      via: "onActivityResumed/Paused"
      purpose: "控制MainActivity页面悬浮球显示"
    - from: "AppLifecycleObserver.java"
      to: "FloatingBallManager(via Broadcast)"
      via: "onActivityStarted/Stopped + 广播"
      purpose: "监听应用前后台切换"
---

<objective>
分析app模块中ActivityLifecycleCallbacks注册点，评估是否可以合并
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/quick-task.md
</execution_context>

<context>
# 注册点位置

## 1. MainActivity.java (line 53)
```java
getApplication().registerActivityLifecycleCallbacks(new android.app.Application.ActivityLifecycleCallbacks() {
    @Override
    public void onActivityResumed(android.app.Activity activity) {
        if (activity == MainActivity.this) {
            FloatingBallManager.getInstance(MainActivity.this).show();
        }
    }

    @Override
    public void onActivityPaused(android.app.Activity activity) {
        if (activity == MainActivity.this) {
            FloatingBallManager.getInstance(MainActivity.this).hide();
        }
    }
    // 其他方法空实现...
});
```
- 作用：MainActivity 可见时显示悬浮球，不可见时隐藏
- 监听方法：onActivityResumed, onActivityPaused
- 目标：仅限 MainActivity

## 2. AppLifecycleObserver.java (通过 App.java:61 注册)
```java
lifecycleObserver = new AppLifecycleObserver(this);
registerActivityLifecycleCallbacks(lifecycleObserver);
```
- 作用：通过 foregroundActivityCount 计数器判断应用进入/退出前台
- 监听方法：onActivityStarted, onActivityStopped
- 发送广播：ACTION_APP_FOREGROUND, ACTION_APP_BACKGROUND
- 目标：全局应用级别
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成代码分析报告</name>
  <files>.planning/quick/15-app-activitylifecyclecallbacks/analysis.md</files>
  <action>
创建分析报告，包含：
1. 注册点清单（位置、功能、监听方法）
2. 职责分析（每个回调的作用）
3. 可合并性评估
4. 合并方案建议（如果可行）
5. 不合并的理由（如果不可行）
  </action>
  <verify>文件存在且包含完整分析</verify>
  <done>分析报告已生成，包含明确的合并建议</done>
</task>

</tasks>

<verification>
- [ ] 分析报告包含所有注册点
- [ ] 每个注册点的职责清晰说明
- [ ] 有明确的合并/不合并建议
- [ ] 如有合并方案，包含具体实现思路
</verification>

<success_criteria>
生成的分析报告能让用户理解：
1. 当前有多少处注册点
2. 每个注册点的作用
3. 建议合并还是保持现状
4. 如果合并，需要如何改动
</success_criteria>

<output>
完成后创建 .planning/quick/15-app-activitylifecyclecallbacks/15-01-SUMMARY.md
</output>
