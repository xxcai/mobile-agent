---
phase: v20-04-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [MIGRATE-04, VERIFY-02, VERIFY-03, VERIFY-04]
user_setup: []

must_haves:
  truths:
    - "项目可以成功编译 (assembleDebug)"
    - "无阻塞性编译错误"
    - "APK 可以正常安装到设备"
    - "聊天功能正常工作"
    - "Android Tools 可以被正常调用"
  artifacts:
    - path: "app/build/outputs/apk/debug/app-debug.apk"
      provides: "可安装的调试 APK"
  key_links: []
---

<objective>
验证重构后的项目可以正常编译和运行
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caixiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/v20-04-verification/v20-04-CONTEXT.md
@.planning/phases/v20-03-code-migration/v20-03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编译验证</name>
  <files>mobile-agent/build.gradle, app/build.gradle, agent/build.gradle</files>
  <action>
运行 `./gradlew assembleDebug` 验证编译成功。检查输出中是否有错误或重要警告。如果编译失败，分析并修复问题。

关键检查点：
1. 确保没有 "cannot find symbol" 错误
2. 确保没有 "package does not exist" 错误
3. 检查重命名后的类是否都存在（MobileAgentApi, NativeMobileAgentApi, NativeMobileAgentApiAdapter 等）
4. 记录编译警告供后续处理
  </action>
  <verify>
<automated>./gradlew assembleDebug</automated>
  </verify>
  <done>
assembleDebug 成功完成，无编译错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>APK 安装和功能测试</name>
  <what-built>重构后的 Android 应用 APK</what-built>
  <how-to-verify>
1. 编译成功后，APK 生成在 app/build/outputs/apk/debug/app-debug.apk
2. 将 APK 安装到 Android 设备：
   - adb install -r app/build/outputs/apk/debug/app-debug.apk
3. 启动应用，进入聊天界面
4. 测试聊天功能：
   - 在输入框输入测试消息
   - 发送消息，验证界面正常显示
5. 测试 Android Tools（通过 Agent 对话触发）：
   - 测试 ShowToast：发送 "请显示一个测试提示"
   - 测试 SearchContacts：发送 "请搜索联系人 张三"
   - 验证 Toast 显示或联系人搜索结果
  </how-to-verify>
  <resume-signal>
Type "approved" if all features work correctly, or describe any issues found
  </resume-signal>
</task>

</tasks>

<verification>
Overall verification checks:
1. assembleDebug 编译成功
2. APK 文件生成在正确位置
3. 聊天界面可以正常启动和交互
4. 消息可以发送和接收
5. Android Tools (ShowToast, SearchContacts) 可以被正常调用
</verification>

<success_criteria>
- assembleDebug 成功
- 无阻塞性编译错误
- APK 可以正常安装到设备
- 聊天功能正常工作
- Android Tools 可以被正常调用
</success_criteria>

<output>
After completion, create `.planning/phases/v20-04-verification/v20-04-01-SUMMARY.md`
</output>
