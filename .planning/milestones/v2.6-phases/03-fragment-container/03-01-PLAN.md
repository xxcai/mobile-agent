---
phase: 03-fragment-container
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-android/src/main/java/com/hh/agent/android/AgentActivity.java
  - agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java
  - agent-android/src/main/java/com/hh/agent/android/AgentFragment.java
  - agent-android/src/main/res/layout/fragment_agent.xml
autonomous: true
requirements:
  - FRAG-01
  - FRAG-02
  - FRAG-03
must_haves:
  truths:
    - 用户点击悬浮球时打开 ContainerActivity（半透明 BottomSheet）
    - ContainerActivity 显示时悬浮球被隐藏
    - AgentFragment 在 ContainerActivity 中正确加载显示
    - 用户可以在 AgentFragment 中发送消息并收到回复
    - 用户可以点击关闭按钮或外部区域关闭 ContainerActivity
    - ContainerActivity 关闭后悬浮球恢复显示
  artifacts:
    - path: "agent-android/src/main/java/com/hh/agent/android/AgentFragment.java"
      provides: "Fragment 封装 AgentActivity UI 逻辑"
      min_lines: 100
    - path: "agent-android/src/main/res/layout/fragment_agent.xml"
      provides: "AgentFragment 布局文件"
    - path: "agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java"
      provides: "加载 AgentFragment 的容器"
      contains: "AgentFragment"
  key_links:
    - from: "FloatingBallManager"
      to: "ContainerActivity"
      via: "点击事件触发 Intent"
      pattern: "startActivity.*ContainerActivity"
    - from: "ContainerActivity"
      to: "AgentFragment"
      via: "FragmentTransaction.replace"
      pattern: "transaction\\.replace.*AgentFragment"
    - from: "ContainerActivity.onCreate"
      to: "FloatingBallManager.hide"
      via: "直接调用隐藏悬浮球"
      pattern: "mFloatingBallManager\\.hide\\(\\)"
    - from: "ContainerActivity.finish"
      to: "FloatingBallManager.show"
      via: "直接调用恢复显示悬浮球"
      pattern: "mFloatingBallManager\\.show\\(\\)"
---

<objective>
将 AgentActivity 界面抽取为 Fragment，集成到 ContainerActivity 中显示，实现点击悬浮球直接打开 Agent 界面。

Purpose: 将原有 AgentActivity 的完整 UI 逻辑抽取为 Fragment，嵌入到半透明的 ContainerActivity 中，实现更好的用户体验。

Output:
- AgentFragment.java: 从 AgentActivity 抽取的 Fragment
- fragment_agent.xml: Fragment 布局文件
- 修改后的 ContainerActivity.java: 加载 AgentFragment
- 修改后的 FloatingBallManager: 跳转到 ContainerActivity
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/caixiao/Workspace/projects/mobile-agent/agent-android/src/main/java/com/hh/agent/android/AgentActivity.java
@/Users/caixiao/Workspace/projects/mobile-agent/agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java
@/Users/caixiao/Workspace/projects/mobile-agent/agent-android/src/main/java/com/hh/agent/floating/FloatingBallManager.java

## Key Interfaces

从 AgentActivity.java 抽取的 UI 组件:
- toolbar: androidx.appcompat.widget.Toolbar
- rvMessages: androidx.recyclerview.widget.RecyclerView
- etMessage: android.widget.EditText
- btnSend: android.widget.ImageButton
- btnVoice: android.widget.ImageButton
- voiceRecordingOverlay: android.view.View

从 AgentActivity.java 抽取的方法需要迁移到 AgentFragment:
- initViews(): 初始化所有 UI 组件
- setupVoiceButtonListener(): 设置语音按钮监听器
- updateVoiceButtonState(boolean recording): 更新语音按钮状态
- checkAndRequestAudioPermission(): 检查录音权限
- onMessagesLoaded(List<Message>): 消息加载回调
- onMessageReceived(Message): 消息接收回调
- onUserMessageSent(Message): 用户消息发送回调
- onError(String): 错误回调
- showLoading()/hideLoading(): 加载状态
- showThinking()/hideThinking(): 思考状态显示

ContainerActivity 关键方法:
- onCreate(): 在此加载 AgentFragment
- finish(): 关闭时恢复悬浮球（已有逻辑）
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 AgentFragment 布局文件</name>
  <files>agent-android/src/main/res/layout/fragment_agent.xml</files>
  <action>
创建 Fragment 布局文件 fragment_agent.xml，复制 activity_main.xml 的内容作为基础。

1. 创建布局文件，复制 activity_main.xml 的根布局和所有子视图
2. 确保布局 ID 与 AgentActivity 中使用的 ID 一致（toolbar, rvMessages, etMessage, btnSend, btnVoice, voiceRecordingOverlay）

注意：使用 activity_main.xml 作为模板，确保所有组件 ID 保持一致。
  </action>
  <verify>
<automated>grep -l "fragment_agent" agent-android/src/main/res/layout/</automated>
</verify>
  <done>fragment_agent.xml 布局文件创建完成，包含与 AgentActivity 相同的 UI 组件</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 AgentFragment 类</name>
  <files>agent-android/src/main/java/com/hh/agent/android/AgentFragment.java</files>
  <action>
创建 AgentFragment 类，从 AgentActivity 抽取 UI 逻辑。

1. 创建 AgentFragment 继承 androidx.fragment.app.Fragment
2. 将 AgentActivity 中的以下内容迁移到 AgentFragment:
   - 所有成员变量（rvMessages, etMessage, btnSend, btnVoice, toolbar, voiceRecordingOverlay, adapter, presenter, isRecording, permissionGranted）
   - initViews() 方法
   - setupVoiceButtonListener() 方法
   - updateVoiceButtonState() 方法
   - checkAndRequestAudioPermission() 方法
   - onMessagesLoaded() / onMessageReceived() / onUserMessageSent() / onError() 回调
   - showLoading() / hideLoading() / showThinking() / hideThinking()
3. 保留 onCreate() 中的逻辑：加载布局、初始化视图、设置语音按钮、加载配置、初始化 Presenter
4. 保留 onDestroy() 中的逻辑：detachView 和 destroy presenter

关键修改点：
- 将 onCreate() 改为 onCreateView()，使用inflater.inflate()加载布局
- 添加 onViewCreated() 方法，在其中调用 initViews()
- 移除 onRequestPermissionsResult()，改用 ActivityCompat.requestPermissions() 并在 Fragment 中处理
  </action>
  <verify>
<automated>grep -l "class AgentFragment" agent-android/src/main/java/com/hh/agent/android/AgentFragment.java</automated>
</verify>
  <done>AgentFragment.java 创建完成，包含完整的 UI 逻辑</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 修改 ContainerActivity 加载 AgentFragment</name>
  <files>agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java</files>
  <action>
修改 ContainerActivity，在 onCreate 中动态加载 AgentFragment。

1. 在 setupLayout() 方法中，修改内容区域创建逻辑
2. 将原来的 createContentArea() 替换为 Fragment 加载代码:
   ```java
   // 获取 FragmentManager
   FragmentManager fragmentManager = getSupportFragmentManager();
   FragmentTransaction transaction = fragmentManager.beginTransaction();

   // 替换内容区域为 AgentFragment
   AgentFragment agentFragment = new AgentFragment();
   transaction.replace(R.id.content_area_container, agentFragment);
   transaction.commit();
   ```
3. 确保内容区域容器有正确的 ID: R.id.content_area_container
4. 保留原有的标题栏和关闭按钮逻辑

注意：需要添加必要的 import:
- androidx.fragment.app.Fragment
- androidx.fragment.app.FragmentManager
- androidx.fragment.app.FragmentTransaction
- com.hh.agent.android.AgentFragment
  </action>
  <verify>
<automated>grep -n "AgentFragment" agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java</automated>
</verify>
  <done>ContainerActivity 加载 AgentFragment，显示 Agent 界面</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 修改悬浮球点击跳转到 ContainerActivity</name>
  <files>agent-android/src/main/java/com/hh/agent/floating/FloatingBallManager.java</files>
  <action>
修改 FloatingBallManager 的 mOnClickListener，使其跳转到 ContainerActivity 而不是 AgentActivity。

在 initialize() 方法中设置点击监听器时，修改为:
```java
mFloatingBallView.setOnSingleTapListener(v -> {
    // 跳转到 ContainerActivity（Fragment 容器）
    Intent intent = new Intent(mContext, ContainerActivity.class);
    intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
    mContext.startActivity(intent);
});
```

移除或注释掉原有的 mOnClickListener 设置逻辑。

确保 ContainerActivity 已在 AndroidManifest.xml 中注册。
  </action>
  <verify>
<automated>grep -n "ContainerActivity" agent-android/src/main/java/com/hh/agent/floating/FloatingBallManager.java</automated>
</verify>
  <done>点击悬浮球直接打开 ContainerActivity（显示 AgentFragment）</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: 在 ContainerActivity 生命周期中控制悬浮球显示</name>
  <files>agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java</files>
  <action>
验证并确认 ContainerActivity 中已有的悬浮球显示/隐藏逻辑：

1. 检查 onCreate() 中是否已调用 mFloatingBallManager.hide() - 隐藏悬浮球
2. 检查 finish() 中是否已调用 mFloatingBallManager.show() - 恢复显示悬浮球
3. 确保关闭按钮点击、返回键按下、外部区域点击都会触发 finish()，从而恢复悬浮球

注意：ContainerActivity 已实现悬浮球控制逻辑，无需额外修改。此任务用于验证并记录已实现的"悬浮球交互"功能（CONTEXT.md 第37-40行锁定决策）。
  </action>
  <verify>
<automated>grep -n "mFloatingBallManager\.\(hide\|show\)" agent-android/src/main/java/com/hh/agent/floating/ContainerActivity.java</automated>
</verify>
  <done>ContainerActivity 生命周期中悬浮球显示/隐藏逻辑已确认实现（onCreate 隐藏，finish 恢复显示）</done>
</task>

</tasks>

<verification>
## Phase-level Verification

1. 编译验证:
   - ./gradlew :agent-android:assembleDebug

2. 功能验证:
   - 点击悬浮球，ContainerActivity 以半透明 BottomSheet 形式展开
   - AgentFragment 正确加载，显示消息列表、输入框、发送按钮
   - 可以发送消息并收到回复
   - 语音按钮功能正常（如果已注入语音识别器）
   - 点击关闭按钮或外部区域，ContainerActivity 关闭，悬浮球恢复显示

3. 原有跳转逻辑验证:
   - 检查是否有其他入口仍跳转到 AgentActivity（保留兼容性）
   - 确保 FloatingBallManager 点击事件跳转到 ContainerActivity

4. 悬浮球交互验证:
   - ContainerActivity 显示时悬浮球被隐藏
   - ContainerActivity 关闭后悬浮球恢复显示
</verification>

<success_criteria>
1. AgentActivity 界面成功抽取为 AgentFragment
2. AgentFragment 正确加载到 ContainerActivity 中显示
3. 原有跳转逻辑保留（悬浮球点击 -> ContainerActivity -> AgentFragment）
4. 悬浮球在 ContainerActivity 显示时隐藏，关闭时恢复
5. 编译通过，功能正常
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.6-phases/03-fragment-container/03-01-SUMMARY.md`
</output>
