---
wave: 1
depends_on: []
files_modified:
  - agent/src/main/java/com/hh/agent/library/AndroidToolManager.java
  - agent/src/main/java/com/hh/agent/library/tools/DisplayNotificationTool.java
  - agent/src/main/java/com/hh/agent/library/tools/ReadClipboardTool.java
  - agent/src/main/java/com/hh/agent/library/tools/TakeScreenshotTool.java
  - app/src/main/assets/tools.json
autonomous: true
---

## Goal

实现 Android 端 function → Executor 注册表（PIPE-03）

## Requirements

- PIPE-03: Android 端注册表实现 (function → Executor 映射)
- ANDROID-01: show_toast 功能 (已存在)
- ANDROID-02: display_notification 功能
- ANDROID-03: read_clipboard 功能

## Tasks

### Task 1: 扩展 AndroidToolManager 注册表

**Modify:** `agent/src/main/java/com/hh/agent/library/AndroidToolManager.java`

在 `initialize()` 方法中添加更多工具注册：

```java
public void initialize() {
    // Register built-in tools
    tools.put("show_toast", new ShowToastTool(getActivity()));
    tools.put("display_notification", new DisplayNotificationTool(getActivity()));
    tools.put("read_clipboard", new ReadClipboardTool(getActivity()));
    tools.put("take_screenshot", new TakeScreenshotTool(getActivity()));
    Log.i("AndroidToolManager", "Registered 4 tools");

    // Load tools.json from assets
    loadToolsConfig();
}
```

- 扩展 `tools` Map 添加 3 个新工具
- 确保 tools.json 中包含对应的 function 枚举

### Task 2: 实现 DisplayNotificationTool

**Create:** `agent/src/main/java/com/hh/agent/library/tools/DisplayNotificationTool.java`

实现通知显示功能：

- 实现 `ToolExecutor` 接口
- 使用 `NotificationManager` 显示通知
- 参数: `title` (String), `content` (String)
- 返回: `{"success": true}` 或错误格式

### Task 3: 实现 ReadClipboardTool

**Create:** `agent/src/main/java/com/hh/agent/library/tools/ReadClipboardTool.java`

实现剪贴板读取功能：

- 实现 `ToolExecutor` 接口
- 使用 `ClipboardManager` 读取文本
- 无参数
- 返回: `{"success": true, "content": "..."}` 或错误格式

### Task 4: 实现 TakeScreenshotTool

**Create:** `agent/src/main/java/com/hh/agent/library/tools/TakeScreenshotTool.java`

实现截屏功能：

- 实现 `ToolExecutor` 接口
- 使用 `MediaStore` 保存截图到相册
- 无参数
- 返回: `{"success": true, "path": "..."}` 或错误格式

### Task 5: 更新 tools.json

**Modify:** `app/src/main/assets/tools.json`

更新 `call_android_tool` 的 function enum：

```json
{
  "name": "call_android_tool",
  "description": "调用 Android 设备功能...",
  "parameters": {
    "type": "object",
    "properties": {
      "function": {
        "type": "string",
        "enum": ["display_notification", "show_toast", "read_clipboard", "take_screenshot"]
      },
      "args": {"type": "object"}
    },
    "required": ["function", "args"]
  }
}
```

## Verification

- [ ] AndroidToolManager.initialize() 注册 4 个工具
- [ ] DisplayNotificationTool 显示系统通知
- [ ] ReadClipboardTool 读取剪贴板内容
- [ ] TakeScreenshotTool 截图并保存
- [ ] tools.json 包含 4 个 function 枚举

## must_haves

1. AndroidToolManager 正确路由 function 名称到对应 Executor
2. 4 个工具都能成功执行并返回 JSON 格式结果
3. 错误处理返回统一的错误格式
