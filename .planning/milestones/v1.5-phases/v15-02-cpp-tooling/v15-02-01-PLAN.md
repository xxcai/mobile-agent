---
phase: v15-02-cpp-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/main/cpp/native_agent.cpp
  - agent/src/main/cpp/src/tools/tool_registry.cpp
  - agent/src/main/java/com/hh/agent/library/NativeAgent.java
autonomous: true
requirements:
  - PIPE-02
  - MODE-01
user_setup: []
must_haves:
  truths:
    - "LLM 只看到 call_android_tool 工具，不直接看到 Android 具体功能"
    - "Android 功能只能通过 call_android_tool 的 function 参数调用"
    - "LLM 可以直接调用工具，无需用户确认"
  artifacts:
    - path: "agent/src/main/cpp/native_agent.cpp"
      provides: "新增 JNI 方法 nativeSetToolsSchema 接收 Java 传递的 tools schema"
    - path: "agent/src/main/java/com/hh/agent/library/NativeAgent.java"
      provides: "新增 nativeSetToolsSchema 方法，初始化时加载 tools.json 并传递"
    - path: "agent/src/main/cpp/src/tools/tool_registry.cpp"
      provides: "新增 register_tools_from_schema 方法，从外部 JSON 注册工具"
  key_links:
    - from: "NativeAgent.java"
      to: "native_agent.cpp"
      via: "JNI nativeSetToolsSchema"
      pattern: "Java_com_hh_agent_library_NativeAgent_nativeSetToolsSchema"
    - from: "native_agent.cpp"
      to: "tool_registry.cpp"
      via: "ToolRegistry instance"
      pattern: "register_tools_from_schema"
    - from: "app/src/main/assets/tools.json"
      to: "NativeAgent.java"
      via: "AssetManager read"
      pattern: "tools\\.json"
---

<objective>
修改 C++ Agent，让 LLM 只看到 call_android_tool 通用管道

Purpose: 实现单一工具原则，LLM 只能通过 call_android_tool 调用 Android 功能
Output: C++ 端只注册 call_android_tool，tools.json schema 从 Java 传递
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caixiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v15-01-tools-schema/v15-01-01-SUMMARY.md
@.planning/phases/v15-02-cpp-tooling/v15-02-CONTEXT.md
@agent/src/main/cpp/native_agent.cpp
@agent/src/main/cpp/src/tools/tool_registry.cpp
@agent/src/main/java/com/hh/agent/library/NativeAgent.java

<!-- Key interfaces from existing codebase -->

From agent/src/main/cpp/src/tools/tool_registry.cpp:
```cpp
class ToolRegistry {
public:
    std::vector<ToolSchema> tool_schemas_;
    void register_builtin_tools();  // Currently registers read_file, write_file, etc.
    void register_tool(const ToolSchema& schema, ToolFunction function);
};
```

From agent/src/main/java/com/hh/agent/library/NativeAgent.java:
```java
public class NativeAgent {
    public static native int nativeInitialize(String configJson);
    public static native String nativeSendMessage(String message);
    // Need to add: nativeSetToolsSchema(String schemaJson)
}
```

From app/src/main/assets/tools.json (created in phase 1):
```json
{
  "tools": [{
    "type": "function",
    "function": {
      "name": "call_android_tool",
      "parameters": {
        "properties": {
          "function": {"enum": ["display_notification", "show_toast", "read_clipboard", "take_screenshot"]},
          "args": {"type": "object"}
        }
      }
    }
  }]
}
```
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add JNI method to receive tools schema</name>
  <files>agent/src/main/cpp/native_agent.cpp, agent/src/main/java/com/hh/agent/library/NativeAgent.java</files>
  <action>
    1. In native_agent.cpp: Add new JNI function `Java_com_hh_agent_library_NativeAgent_nativeSetToolsSchema` that:
       - Accepts jstring schemaJson parameter
       - Parses the JSON
       - Calls ToolRegistry to register the tools from schema

    2. In NativeAgent.java: Add new public method:
       - `public static native void nativeSetToolsSchema(String schemaJson)`
       - This will be called after nativeInitialize to pass the tools schema

    The JNI function should parse the tools array from schemaJson and register each tool's schema.
  </action>
  <verify>
    <automated>grep -n "nativeSetToolsSchema" agent/src/main/cpp/native_agent.cpp agent/src/main/java/com/hh/agent/library/NativeAgent.java</automated>
  </verify>
  <done>JNI method added, Java can call nativeSetToolsSchema to pass tools schema</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modify ToolRegistry to accept external schema</name>
  <files>agent/src/main/cpp/src/tools/tool_registry.cpp</files>
  <action>
    1. Add new method to ToolRegistry:
       - `void register_tools_from_schema(const nlohmann::json& schema)`
       - Parses the tools array from schema JSON
       - For each tool in array, extracts name, description, parameters
       - Registers tool with empty function (actual execution via Android callback)

    2. Modify register_builtin_tools() to NOT register show_toast (keep read_file, write_file as they are separate from Android tools)

    3. The call_android_tool will have empty function since actual execution goes through Android callback mechanism (g_android_tools.call_tool)
  </action>
  <verify>
    <automated>grep -n "register_tools_from_schema" agent/src/main/cpp/src/tools/tool_registry.cpp</automated>
  </verify>
  <done>ToolRegistry can register tools from external JSON schema</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify tools.json is loaded and passed</name>
  <files>agent/src/main/java/com/hh/agent/library/NativeAgent.java</files>
  <action>
    Add code in NativeAgent initialization flow to:
    1. Read tools.json from assets using AssetManager
    2. Call nativeSetToolsSchema() with the JSON content

    This ensures the tools schema defined in phase 1 is actually used by the C++ agent.
  </action>
  <verify>
    <automated>grep -n "tools.json\|setToolsSchema" agent/src/main/java/com/hh/agent/library/NativeAgent.java</automated>
  </verify>
  <done>Java side loads tools.json and passes to native layer</done>
</task>

</tasks>

<verification>
- C++ code compiles without errors
- JNI method signatures match between Java and C++
- tools.json content is correctly parsed and registered
- Only call_android_tool is exposed to LLM (not individual Android functions)
</verification>

<success_criteria>
- LLM sees only call_android_tool in tools list
- Android tools (display_notification, show_toast, etc.) are only accessible via call_android_tool
- Fully autonomous mode: LLM directly calls tools without user confirmation (already satisfied by existing callback mechanism)
</success_criteria>

<output>
After completion, create `.planning/phases/v15-02-cpp-tooling/v15-02-01-SUMMARY.md`
</output>
