---
phase: 06-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/CMakeLists.txt
autonomous: true
requirements:
  - CLEAN-04
  - CLEAN-05
  - CLEAN-03
---

<objective>
清理 agent 模块，构建配置和代码简化
</objective>

<context>
@/Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/CMakeLists.txt

现有构建配置包含硬编码的 Conan 路径，需要简化为更通用的配置。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除硬编码路径和简化链接配置</name>
  <files>/Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/CMakeLists.txt</files>
  <action>
分析并简化 CMakeLists.txt：

1. 移除硬编码的个人路径 (第111-119行):
   - 移除 libcurl_PACKAGE_FOLDER_RELEASE, sqlite3_PACKAGE_FOLDER_RELEASE, fmt_PACKAGE_FOLDER_RELEASE 的硬编码路径
   - 这些应该使用 find_package 生成的变量或导入目标

2. 简化 target_link_libraries (第128-135行):
   - 优先使用 Conan 生成的导入目标 (如 CURL::libcurl)
   - 如果导入目标不可用，使用 ${CMAKE_CURRENT_SOURCE_DIR}/../../.. 相对路径

3. 确保 find_package 在 ANDROID 条件下正常工作

保留必要的配置:
- find_package 调用
- target_include_directories
- target_link_libraries
  </action>
  <verify>
<automated>cd /Users/caixiao/Workspace/projects/mobile-agent/agent && ./gradlew :agent:compileDebugArm64v8aC++ 2>&1 | tail -30</automated>
  </verify>
  <done>CMakeLists.txt 不包含硬编码的个人路径，构建成功</done>
</task>

<task type="auto">
  <name>Task 2: 检查 Android include 和链接逻辑是否冗余</name>
  <files>/Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp/CMakeLists.txt</files>
  <action>
检查 Android 特定的 include 和链接配置：

1. 分析第90-124行的 Android-specific 配置:
   - 检查 target_include_directories 是否冗余
   - 检查 include() 数据文件是否必要
   - 验证链接配置是否与 find_package 一致

2. 优化建议:
   - 如果某些 include 路径已被 find_package 覆盖，可以移除
   - 如果链接配置可使用导入目标，优先使用

注意：不需要完全删除 Android 配置，而是确保其简洁和正确
  </action>
  <verify>
<automated>cd /Users/caixiao/Workspace/projects/mobile-agent/agent && ./gradlew :agent:compileDebugArm64v8aC++ 2>&1 | tail -30</automated>
  </verify>
  <done>Android include 和链接逻辑已优化，构建成功</done>
</task>

<task type="auto">
  <name>Task 3: 验证构建</name>
  <files>/Users/caixiao/Workspace/projects/mobile-agent/agent/src/main/cpp</files>
  <action>
执行完整构建验证清理后的代码可以正常编译:
- 运行 assembleDebug 构建
- 检查是否有编译警告
  </action>
  <verify>
<automated>cd /Users/caixiao/Workspace/projects/mobile-agent/agent && ./gradlew :agent:assembleDebug 2>&1 | tail -50</automated>
  </verify>
  <done>构建成功，无错误</done>
</task>

</tasks>

<verification>
- CMakeLists.txt 不包含硬编码的个人路径
- 构建成功完成
- 无编译警告
</verification>

<success_criteria>
1. CMakeLists.txt 配置简化完成
2. 构建可以正常完成
3. 无调试代码残留
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent/06-01-SUMMARY.md`
</output>
