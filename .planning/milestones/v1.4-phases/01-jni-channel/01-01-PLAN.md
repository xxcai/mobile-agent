---
wave: 1
depends_on: []
files_modified:
  - agent/src/main/cpp/native_agent.cpp
  - agent/src/main/java/com/hh/agent/library/NativeAgent.java
autonomous: true
---

# Plan: JNI 回调通道

**Objective:** C++ Agent 提供调用 Android 的统一接口

## Requirements
- TOOL-01

## Tasks

### T1: 创建 C++ AndroidToolCallback 接口
**File:** `agent/src/main/cpp/android_tools.cpp` (新建)

- 定义 `AndroidToolCallback` 抽象类
- 实现 `call_tool(tool_name, args)` 同步方法
- 返回 JSON 格式结果
- 在 `MobileAgent` 中持有回调实例

### T2: 修改 native_agent.cpp 添加 JNI 回调注册
**File:** `agent/src/main/cpp/native_agent.cpp`

- 添加 JNI 方法 `nativeRegisterAndroidToolCallback`
- Java 层传入 JNI 回调接口
- C++ 存储 Java 对象的全局引用
- 实现 `call_android_tool` 调用 Java 方法

### T3: 创建 Java 侧回调接口
**File:** `agent/src/main/java/com/hh/agent/library/AndroidToolCallback.java` (新建)

- 定义 `AndroidToolCallback` 接口
- 方法: `String callTool(String toolName, String argsJson)`

### T4: 修改 NativeAgent.java 注册回调
**File:** `agent/src/main/java/com/hh/agent/library/NativeAgent.java`

- 添加 `nativeRegisterAndroidToolCallback` native 方法
- 实现回调接口，将调用转发给 Java 层

---

## Verification Criteria

1. C++ 代码可以调用 `call_android_tool("test", {"msg": "hello"})`
2. 返回 JSON 格式: `{"success": true, "result": "..."}`
3. 工具不存在时返回: `{"success": false, "error": "tool_not_found"}`
4. 参数错误时返回: `{"success": false, "error": "invalid_args"}`

## Success Metrics

- [ ] `call_android_tool` 接口可用
- [ ] 同步返回 JSON 结果
- [ ] JNI 层正确传递参数
