---
phase: v2.9-05
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-android/src/main/java/com/hh/agent/android/di/AgentContainer.java
  - agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java
  - agent-android/src/main/java/com/hh/agent/android/MobileAgentApplication.java
autonomous: true
requirements:
  - DI-01
must_haves:
  truths:
    - 开发者可通过 AgentContainer.getInstance() 获取统一入口
    - MainPresenter 通过构造函数接收依赖，而非直接创建
    - 容器在 Application 启动时初始化
    - 可在单元测试中 mock 容器中的依赖
  artifacts:
    - path: "agent-android/src/main/java/com/hh/agent/android/di/AgentContainer.java"
      provides: "依赖注入容器"
      min_lines: 40
      exports: ["init(Context)", "getInstance()", "getMobileAgentApi()", "getStreamingManager()", "getThreadPoolManager()"]
    - path: "agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java"
      provides: "改造后的 Presenter 支持构造函数注入"
      constructor_params: ["MobileAgentApi", "StreamingManager"]
    - path: "agent-android/src/main/java/com/hh/agent/android/MobileAgentApplication.java"
      provides: "容器初始化"
      contains: "AgentContainer.init"
  key_links:
    - from: "MobileAgentApplication"
      to: "AgentContainer"
      via: "onCreate() 中调用 init()"
    - from: "MainPresenter"
      to: "AgentContainer"
      via: "构造函数接收依赖"
---

<objective>
创建手写的依赖注入容器 AgentContainer，统一管理核心组件依赖 (MobileAgentApi, StreamingManager, ThreadPoolManager)，提高可测试性。
</objective>

<execution_context>
@/Users/caixiao/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/milestones/v2.9-phases/05-di-container/v2.9-05-CONTEXT.md
@.planning/quick/260317-h5r-agent-android/architecture-analysis.md (第 504-560 行：DI 容器示例代码)
@.planning/milestones/v2.9-phases/03-thread-pool/v2.9-03-01-SUMMARY.md
@agent-android/src/main/java/com/hh/agent/android/presenter/StreamingManager.java
@agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java
@agent-android/src/main/java/com/hh/agent/android/MobileAgentApplication.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 AgentContainer 依赖注入容器</name>
  <files>agent-android/src/main/java/com/hh/agent/android/di/AgentContainer.java</files>
  <read_first>
    - architecture-analysis.md (504-560行：DI容器示例代码)
    - agent-android/src/main/java/com/hh/agent/android/presenter/StreamingManager.java
    - agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java
  </read_first>
  <action>
    1. 在 `agent-android/src/main/java/com/hh/agent/android/di/` 目录下创建 AgentContainer.java
    2. 实现单例模式 `private static AgentContainer instance`
    3. 容器管理 3 个核心依赖:
       - `private final MobileAgentApi mobileAgentApi`
       - `private final StreamingManager streamingManager`
       - `private final ThreadPoolManager threadPoolManager`
    4. 构造函数 `private AgentContainer(Context context)` 中创建所有依赖:
       - `this.mobileAgentApi = createMobileAgentApi(context)`
       - `this.threadPoolManager = new ThreadPoolManager()`
       - `this.streamingManager = new StreamingManager(mobileAgentApi)`
    5. 提供静态方法:
       - `public static void init(Context context)` - 初始化容器
       - `public static AgentContainer getInstance()` - 获取单例
    6. 提供实例方法获取依赖:
       - `public MobileAgentApi getMobileAgentApi()`
       - `public StreamingManager getStreamingManager()`
       - `public ThreadPoolManager getThreadPoolManager()`
    7. 创建私有方法 `createMobileAgentApi(Context context)` 创建 NativeMobileAgentApiAdapter 实例
  </action>
  <verify>
    <automated>grep -n "class AgentContainer" agent-android/src/main/java/com/hh/agent/android/di/AgentContainer.java && grep -n "public static void init" agent-android/src/main/java/com/hh/agent/android/di/AgentContainer.java && grep -n "getMobileAgentApi\|getStreamingManager\|getThreadPoolManager" agent-android/src/main/java/com/hh/agent/android/di/AgentContainer.java</automated>
  </verify>
  <acceptance_criteria>
    - [x] AgentContainer.java 文件存在于 `com.hh.agent.android.di` 包
    - [x] 包含 `init(Context)` 静态方法
    - [x] 包含 `getInstance()` 静态方法
    - [x] 包含 `getMobileAgentApi()` 方法返回 MobileAgentApi
    - [x] 包含 `getStreamingManager()` 方法返回 StreamingManager
    - [x] 包含 `getThreadPoolManager()` 方法返回 ThreadPoolManager
    - [x] 构造函数中创建并持有 3 个核心依赖的实例
  </acceptance_criteria>
  <done>AgentContainer.java 创建完成，包含 3 个核心组件的注册和获取方法</done>
</task>

<task type="auto">
  <name>Task 2: 改造 MainPresenter 构造函数支持依赖注入</name>
  <files>agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java</files>
  <read_first>
    - agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java
    - architecture-analysis.md (第 548-576 行：MainPresenter 使用示例)
  </read_first>
  <action>
    1. 修改 MainPresenter 构造函数签名，从:
       `public MainPresenter(Context context)`
       改为:
       `public MainPresenter(MobileAgentApi api, StreamingManager streamingManager, Context context)`
    2. 将类内的依赖字段改为 final:
       - `private final MobileAgentApi mobileAgentApi`
       - `private final StreamingManager streamingManager`
    3. 删除或保留 `private final Context context`
    4. 在构造函数中赋值:
       - `this.mobileAgentApi = api`
       - `this.streamingManager = streamingManager`
    5. 删除直接创建 `new NativeMobileAgentApiAdapter()` 的代码（如果存在）
    6. 删除直接创建 `new StreamingManager()` 的代码（如果存在）
    7. 添加静态工厂方法 `public static MainPresenter create()`:
       ```java
       public static MainPresenter create() {
           AgentContainer container = AgentContainer.getInstance();
           return new MainPresenter(
               container.getMobileAgentApi(),
               container.getStreamingManager(),
               container.getContext()  // 需要添加 getContext() 到容器
           );
       }
       ```
    8. 如果容器没有 getContext() 方法，需要添加该方法到 AgentContainer
  </action>
  <verify>
    <automated>grep -n "public MainPresenter(MobileAgentApi" agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java && grep -n "public static MainPresenter create()" agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java</automated>
  </verify>
  <acceptance_criteria>
    - [x] MainPresenter 构造函数接受 MobileAgentApi 和 StreamingManager 参数
    - [x] 包含 `public static MainPresenter create()` 静态工厂方法
    - [x] 静态工厂方法从 AgentContainer 获取依赖并创建实例
    - [x] 类内依赖字段为 final 类型
    - [x] 不再直接 new NativeMobileAgentApiAdapter()（如果之前有）
    - [x] 不再直接 new StreamingManager()（如果之前有）
  </acceptance_criteria>
  <done>MainPresenter 支持构造函数注入，可通过 AgentContainer.create() 创建实例</done>
</task>

<task type="auto">
  <name>Task 3: 在 Application 中初始化 AgentContainer</name>
  <files>agent-android/src/main/java/com/hh/agent/android/MobileAgentApplication.java</files>
  <read_first>
    - agent-android/src/main/java/com/hh/agent/android/MobileAgentApplication.java
  </read_first>
  <action>
    1. 在 MobileAgentApplication 类中添加 import:
       `import com.hh.agent.android.di.AgentContainer`
    2. 在 onCreate() 方法开头添加容器初始化:
       ```java
       @Override
       public void onCreate() {
           super.onCreate();
           // 初始化依赖注入容器
           AgentContainer.init(this);
           // ... 其余原有代码
       }
       ```
    3. 确保容器初始化在其他组件创建之前执行
  </action>
  <verify>
    <automated>grep -n "AgentContainer.init" agent-android/src/main/java/com/hh/agent/android/MobileAgentApplication.java</automated>
  </verify>
  <acceptance_criteria>
    - [x] MobileAgentApplication.onCreate() 中调用 AgentContainer.init(this)
    - [x] import 语句已添加
    - [x] 容器初始化在 super.onCreate() 之后执行
  </acceptance_criteria>
  <done>Application 启动时自动初始化 DI 容器</done>
</task>

</tasks>

<verification>
- [x] AgentContainer.java 存在且包含所有必需方法
- [x] MainPresenter 构造函数已改造为接收注入的依赖
- [x] MobileAgentApplication.onCreate() 初始化容器
- [x] 代码可编译通过（无语法错误）
</verification>

<success_criteria>
- 开发者可通过 `AgentContainer.getInstance().getMobileAgentApi()` 获取 MobileAgentApi
- 开发者可通过 `AgentContainer.getInstance().getStreamingManager()` 获取 StreamingManager
- 开发者可通过 `AgentContainer.getInstance().getThreadPoolManager()` 获取 ThreadPoolManager
- MainPresenter 可通过 `MainPresenter.create()` 或构造函数创建
- 单元测试时可 mock AgentContainer 提供测试替身
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.9-phases/05-di-container/v2.9-05-01-SUMMARY.md`
</output>
