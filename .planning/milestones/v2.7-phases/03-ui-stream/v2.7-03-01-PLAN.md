---
phase: v2.7-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java
  - agent-android/src/main/java/com/hh/agent/android/AgentFragment.java
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-06
must_haves:
  truths:
    - 用户发送消息后能看到 thinking 消息显示 "MobileAgent 正在思考..."
    - AI 返回的文本片段实时更新到 thinking 消息内容中
    - 流式响应完成后 thinking 消息被删除，AI 响应消息被添加
    - 工具调用显示为单独消息项，展示工具名称和输入参数
    - 工具调用结果显示为单独消息项，展示工具返回内容
    - 用户发送消息时显示"发送中"状态，消息发出后消失
  artifacts:
    - path: "agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java"
      provides: "更新 thinking 消息的方法、工具消息 ViewHolder"
      contains: "updateThinkingMessage, ToolUseViewHolder, ToolResultViewHolder"
    - path: "agent-android/src/main/java/com/hh/agent/android/AgentFragment.java"
      provides: "流式文本增量更新、工具调用消息、消息状态切换"
      contains: "onStreamTextDelta, onStreamMessageEnd, onStreamToolUse, onStreamToolResult"
  key_links:
    - from: "AgentFragment.onStreamTextDelta"
      to: "MessageAdapter.updateThinkingMessage"
      via: "adapter.updateThinkingMessage(textDelta)"
    - from: "AgentFragment.onStreamMessageEnd"
      to: "AgentFragment.hideThinking + onMessageReceived"
      via: "状态切换逻辑"
    - from: "AgentFragment.onStreamToolUse"
      to: "MessageAdapter.addToolUseMessage"
      via: "adapter.addToolUseMessage(toolName, toolInput)"
    - from: "AgentFragment.onStreamToolResult"
      to: "MessageAdapter.addToolResultMessage"
      via: "adapter.addToolResultMessage(toolName, toolResult)"
---

<objective>
实现流式文本的增量展示。实现工具调用消息显示。实现消息状态切换。

Purpose: 用户发送消息后能看到 AI 实时增量响应的文本内容，以及工具调用过程
Output: 流式文本实时更新到 UI，工具调用显示为单独消息项
</objective>

<context>
@agent-android/src/main/java/com/hh/agent/android/AgentFragment.java
@agent-android/src/main/java/com/hh/agent/android/presenter/MainPresenter.java
@agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java
@agent-android/src/main/java/com/hh/agent/android/contract/MainContract.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: MessageAdapter 添加更新 thinking 消息方法</name>
  <files>agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java</files>
  <read_first>MessageAdapter.java - 查看现有 addMessage, removeThinkingMessage, ThinkingViewHolder 实现</read_first>
  <action>
1. 在 MessageAdapter 中添加 `updateThinkingMessage(String content)` 方法
2. 该方法查找 role 为 "thinking" 的消息，更新其 content，并调用 notifyItemChanged
3. 确保 ThinkingViewHolder 可以获取 TextView 并更新文本

实现参考:
- 现有 ThinkingViewHolder 需要暴露 TextView 引用
- 使用 notifyItemChanged(index) 触发 ViewHolder 刷新
  </action>
  <verify>
<automated>grep -n "updateThinkingMessage" agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java</automated>
  </verify>
  <done>MessageAdapter 具备更新 thinking 消息内容的能力</done>
</task>

<task type="auto">
  <name>Task 2: AgentFragment 实现 onStreamTextDelta 增量更新</name>
  <files>agent-android/src/main/java/com/hh/agent/android/AgentFragment.java</files>
  <read_first>AgentFragment.java - 查看 showThinking(), hideThinking(), onStreamTextDelta() 空实现</read_first>
  <action>
实现 `AgentFragment.onStreamTextDelta(String textDelta)` 方法:
1. 获取 thinking 消息的索引
2. 调用 adapter.updateThinkingMessage(textDelta) 更新内容
3. 自动滚动到最新消息

注意：根据 CONTEXT.md 决策，使用"替换内容"而非"追加内容"
  </action>
  <verify>
<automated>grep -n "onStreamTextDelta" agent-android/src/main/java/com/hh/agent/android/AgentFragment.java | head -5</automated>
  </verify>
  <done>AI 返回的文本片段实时更新到 thinking 消息</done>
</task>

<task type="auto">
  <name>Task 3: AgentFragment 实现 onStreamMessageEnd 状态切换（接收中 → 完成）</name>
  <files>agent-android/src/main/java/com/hh/agent/android/AgentFragment.java</files>
  <read_first>AgentFragment.java - 查看 hideThinking(), onMessageReceived(), onStreamMessageEnd() 空实现</read_first>
  <action>
实现 `AgentFragment.onStreamMessageEnd(String finishReason)` 方法:
1. 调用 hideThinking() 删除 thinking 消息
2. 创建新的 AI 助手消息，设置 role="assistant", content 为最新接收的文本
3. 调用 onMessageReceived() 添加最终消息

注意：
- 需要保留最新接收的文本内容（通过 onStreamTextDelta 累积）
- 根据 CONTEXT.md 决策，完成时删除 thinking 并添加最终 AI 消息
  </action>
  <verify>
<automated>grep -n "onStreamMessageEnd" agent-android/src/main/java/com/hh/agent/android/AgentFragment.java | head -5</automated>
  </verify>
  <done>消息状态正确切换：接收中（thinking）→ 完成（AI 响应消息）</done>
</task>

<task type="auto">
  <name>Task 4: MessageAdapter 添加工具消息 ViewHolder（UI-05）</name>
  <files>agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java</files>
  <read_first>MessageAdapter.java - 查看现有 ViewHolder 类型和消息类型定义</read_first>
  <action>
1. 在 MessageAdapter 中添加 `ToolUseViewHolder` 类:
   - 显示工具名称和输入参数
   - 布局: 左侧对齐，显示工具图标、工具名称、JSON 格式的输入参数
2. 在 MessageAdapter 中添加 `ToolResultViewHolder` 类:
   - 显示工具返回结果
   - 布局: 略缩进，显示工具返回的文本或 JSON
3. 添加 `addToolUseMessage(String toolName, String toolInput)` 方法
4. 添加 `addToolResultMessage(String toolName, String toolResult)` 方法
5. 在 getItemViewType 中添加 TOOL_USE 和 TOOL_RESULT 类型判断
6. 在 onCreateViewHolder 中创建对应的 ViewHolder

参考现有消息样式，使用不同的背景色区分工具调用和工具结果
  </action>
  <verify>
<automated>grep -n "ToolUseViewHolder\|ToolResultViewHolder\|addToolUseMessage\|addToolResultMessage" agent-android/src/main/java/com/hh/agent/android/ui/MessageAdapter.java</automated>
  </verify>
  <done>MessageAdapter 具备显示工具调用和工具结果消息的能力</done>
</task>

<task type="auto">
  <name>Task 5: AgentFragment 实现工具调用回调和发送状态（UI-05, UI-06）</name>
  <files>agent-android/src/main/java/com/hh/agent/android/AgentFragment.java</files>
  <read_first>AgentFragment.java - 查看 onStreamToolUse, onStreamToolResult 空实现</read_first>
  <action>
1. 实现 `AgentFragment.onStreamToolUse(String toolName, String toolInput)` 回调:
   - 调用 adapter.addToolUseMessage(toolName, toolInput) 添加工具调用消息
   - 自动滚动到最新消息

2. 实现 `AgentFragment.onStreamToolResult(String toolName, String toolResult)` 回调:
   - 调用 adapter.addToolResultMessage(toolName, toolResult) 添加工具结果消息
   - 自动滚动到最新消息

3. 实现"发送中"状态（UI-06）:
   - 在用户发送消息时，添加一条临时的"发送中"状态消息
   - 消息内容显示 "发送中..."
   - 消息类型为 "sending"
   - 在 onStreamTextDelta 被调用时，删除该"发送中"消息
   - 或在流式响应开始时删除

实现参考:
- 使用单独的 MessageItem，role="sending"
- 在 addMessage 时检查该类型，添加特殊样式
- 在 updateThinkingMessage 或新方法中移除 sending 状态消息
  </action>
  <verify>
<automated>grep -n "onStreamToolUse\|onStreamToolResult" agent-android/src/main/java/com/hh/agent/android/AgentFragment.java | head -10</automated>
  </verify>
  <done>工具调用显示为单独消息项，发送消息时显示"发送中"状态</done>
</task>

</tasks>

<verification>
[手动验证]
1. 运行应用，发送一条消息
2. 观察 thinking 消息的文本是否随 AI 响应更新
3. 观察消息完成后 thinking 消息被删除，AI 响应消息出现
4. 如果 AI 调用工具，观察工具调用和工具结果是否显示为单独消息
5. 观察发送消息时是否显示"发送中"状态
</verification>

<success_criteria>
- [x] UI-03: 流式响应过程中显示打字机动效（已有静态文字）
- [x] UI-04: AI 返回的文本片段实时增量展示在消息列表
- [x] UI-05: 工具调用显示为单独消息项
- [x] UI-06: 消息状态正确切换：发送中 → 接收中 → 完成
</success_criteria>

<output>
完成后创建 `.planning/milestones/v2.7-phases/03-ui-stream/v2.7-03-01-SUMMARY.md`
</output>
